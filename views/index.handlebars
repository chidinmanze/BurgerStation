
<div class="container">
    <div class="overlay"></div>
   


    <div class="jumbotron bg-color">
        <h1 id="color-override">Burger Station</h1>
        <p id="color-override">Make your own burger, then chow down</p>
        <br>
        <h2 id="color-override">Make a Burger</h2>
        <panel>
            <form class="create-form">
                <div class="form-group">
                    <label for="newbike" id="color-override">Make a Burger:</label>
                    <input type="text" id="newburger" name="burger">
                    {{!--Create a button in index.handlebars that will submit the user input into the database.--}}
                    <button type="submit" class="btn btn-dark" id="addburger">
                        <span class="fa fa-thumbs-up"></span>
                    </button>
                </div>
            </form>
        </panel>
    </div>
    <!-- /jumbotron -->

    <div class="row">
        <!-- Available Bikes -->
        <div class="col-sm-6">
            <div class="panel panel-default">
                <div class="panel-heading" id="lefthead">
                    <strong>Burgers Ready</strong>
                </div>
                <div class="panel-body">
                    <ul>
                        {{#each burgers}}
                        {{#unless eaten}}
                        <span>
                            <strong>
                                {{ this.id }}
                                -
                                {{ this.burger_name }}
                            </strong>
                        </span>
                        <button type="button" class="rentbike btn btn-dark" id="eatbutton" data-id="{{ this.id }}"
                            data-newEaten="{{eaten}}">
                            <span class="fa fa-thumbs-up"></span>
                        </button>
                        <hr>
                        {{/unless}}
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
        <!-- Checked out bikes -->
        <div class="col-sm-6">
            <div class="panel panel-default">
                <div class="panel-heading" id="righthead">
                    <strong>Burgers Gone</strong>
                </div>
                <div class="panel-body">
                    <ul>
                        {{#each burgers}}
                        {{#if eaten}}
                        <span>
                            <strong>
                                {{ this.id }}
                                -
                                {{ this.burger_name }}
                            </strong>
                        </span>
                        <button type="button" class="returnbike btn btn-dark" id="returnbutton" data-id="{{ this.id }}"
                            data-newEaten="{{eaten}}">
                            <span class="fas fa-undo"></span>
                        </button>
                        <hr>
                        {{/if}}
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <!-- /row -->

</div>
<!-- /container -->